---
---
<section class="staldren-section" id="staldren">
  <div class="staldren-container">
    <div class="staldren-text">
      <h2>Staldren® — <span class="accent">«чистый хлев»</span></h2>
      <p>Зоогигиеническое средство в виде порошка для всех видов сельскохозяйственных животных и птиц. Разработано датской компанией J. N. Jorenku.</p>
      <p><strong>Эффекты:</strong> мощный осушитель подстилки, сильное дезинфицирующее и дезодорирующее действие. Экспортируется во многие страны мира.</p>
      <p>Дистрибуция в РФ — через партнёрскую сеть.</p>
      <p><a href="http://www.staldren.site/" class="staldren-link" target="_blank" rel="noopener">🌐 Подробнее на staldren.site →</a></p>
    </div>
    <div class="staldren-visual">
      <div class="big-icon">🧪</div>
      <h3>Staldren®</h3>
      <p style="opacity:0.8;">Сухость. Чистота. Здоровое поголовье.</p>
    </div>
  </div>
</section>

<style>
  .staldren-section {
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #000;
    padding: 0 5%;
    box-sizing: border-box;
  }
  .staldren-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
    max-width: 1200px;
    width: 100%;
  }
  .staldren-text h2 {
    font-size: 2.5rem;
    font-weight: 900;
    margin-bottom: 1rem;
  }
  .accent { color: var(--accent); }
  .staldren-text p {
    opacity: 0.8;
    margin-bottom: 1rem;
  }
  .staldren-link {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
    font-size: 1.1rem;
  }
  .staldren-visual {
    background: linear-gradient(135deg, #1e3a8a, #0f172a);
    border-radius: 24px;
    padding: 50px;
    text-align: center;
    border: 1px solid var(--border);
  }
  .staldren-visual .big-icon {
    font-size: 5rem;
    margin-bottom: 15px;
  }
  .staldren-visual h3 {
    font-size: 1.8rem;
    font-weight: 900;
    color: var(--accent);
  }
  @media (max-width: 768px) {
    .staldren-container {
      grid-template-columns: 1fr;
      gap: 30px;
    }
  }
</style>